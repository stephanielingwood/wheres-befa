'use strict';

function Airplanes (db) {
  this.db = db;
}

Airplanes.prototype.get = function (callback) {
  // TODO: refactor this and handler to allow for testing. decouple business logic from platform-specific logic.
};
